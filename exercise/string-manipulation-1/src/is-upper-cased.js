/* eslint-disable no-unused-vars */

function isUpperCased (word){ 
    return word === word.toUpperCase();
} 
// define a function 
// condition if all characters in the word are uppercased
// if so return true
// otherwise false 